# S2.5 Plan — CSV Upload Backend

**Story:** s2.5 | **Size:** S (3 SP) | **Date:** 2026-07-01
**Dependencies:** s2.2 (IScenarioRepository + scenarios router pattern)

---

## Task List

### T1 — CSV upload endpoint + happy path (M)

**Description:** Create the `POST /api/v1/scenarios/upload-csv` endpoint that accepts a CSV file via `multipart/form-data`, parses it with `csv.DictReader`, validates required columns, and creates Scenario + Client + Invoice rows on success.

**Files:**
- `backend/tests/test_csv_upload.py` — CREATE: integration tests
- `backend/app/routers/scenarios.py` — MODIFY: add POST /upload-csv

**TDD cycle:**
- RED: Write integration test `test_upload_csv_valid_returns_201` — POST a valid CSV, assert 201 + ScenarioSummary with client_count=3
- RED: Write integration test `test_upload_csv_missing_columns_returns_422` — POST CSV without `amount` column, assert 422 + detail message listing missing cols
- RED: Write integration test `test_upload_csv_empty_file_returns_422` — POST empty file, assert 422
- RED: Write integration test `test_upload_csv_malformed_returns_422` — POST non-CSV content, assert 422
- GREEN: Implement endpoint: parse CSV with `csv.DictReader`, validate required columns (`client_name`, `amount`, `due_date`, `invoice_id`), create Scenario (source=`csv_upload`) + Client + Invoice entities, return ScenarioSummary (201)
- REFACTOR: Extract `_parse_csv()` and `_validate_csv_columns()` helpers if needed

**AC reference:** story.md § All 4 Gherkin scenarios

**Verification:**
- `rai gate check gate-tests --scope backend/tests/test_csv_upload.py`
- `rai gate check gate-lint`
- `rai gate check gate-types`

**Size:** M | **Depends on:** —

---

### T2 — Manual E2E verification (S)

**Description:** Run backend server + frontend, upload a valid CSV via the file picker, and verify the scenario appears in the grid.

**Files:** None (manual)

**Verification:** Manual — no automated gate

**Size:** S | **Depends on:** T1

---

## Execution Order

```
T1 → T2
```

T1 is the only implementation task. T2 is manual smoke test after T1 passes all gates.

## Gate Policy

| When | Command | Scope |
|------|---------|-------|
| Per task | `rai gate check gate-tests --scope backend/tests/test_csv_upload.py` | New test file only |
| Per task | `rai gate check gate-lint` | Full project |
| Per task | `rai gate check gate-types` | Full project |
| End of story | `rai gate check gate-tests --scope backend/tests/` | All backend tests |
| MR creation | `/rai-mr-create` | Full suite |

## Risks

| Risk | Mitigation |
|------|------------|
| CSV parsing edge cases (BOM, encoding) | Test with UTF-8 CSV only; BOM/encoding out of scope per E2 design |
| ORM flush ordering (Scenario → Client → Invoice) | Use same session, add in dependency order |
| `csv_upload` source value vs existing enum | String literal — no enum change needed |

## Duration Tracking

| Task | Started | Completed | Duration |
|------|---------|-----------|----------|
| T1 | | | |
| T2 | | | |
| **Total** | | | |
