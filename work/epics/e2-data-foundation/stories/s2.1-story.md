# s2.1 — Domain Schema & Migrations

**Epic:** E2 — Data Foundation  
**Branch:** `s2.1-domain-schema` (from `e2-data-foundation`)  
**Size:** S  
**Owner:** —  
**Depends on:** E1 merged

---

## User story

> As a developer, I want all domain entities represented as PostgreSQL tables with proper relationships so that all subsequent epics can persist and query real data.

---

## Acceptance criteria

- [ ] `alembic upgrade head` creates: `scenarios`, `clients`, `invoices`, `payments`, `scores`, `communications`, `contact_results`
- [ ] All tables use UUID primary keys
- [ ] Foreign key relationships are enforced at the DB level (not just application level)
- [ ] Partial unique index on `scenarios (status) WHERE status = 'active'` exists and is enforced
- [ ] Three seed scenarios are inserted by `0003_seed_scenarios.py` migration
- [ ] `alembic downgrade -1` successfully reverses the migration
- [ ] Domain Pydantic entities (no SQLAlchemy) are defined in `app/domain/entities/`
- [ ] ORM models are defined in `app/adapters/persistence/models.py` — separate from domain entities
- [ ] `app/adapters/persistence/mappers.py` converts ORM ↔ domain in both directions
- [ ] `mypy app/` exits 0 with all new files included
- [ ] ADR-002 written
