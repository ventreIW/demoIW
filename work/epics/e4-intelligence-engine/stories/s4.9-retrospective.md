# Retrospective: S4.9 — Score Persistence (IScoreRepository + adapter)

**Dates:** 2026-07-27 (single session)
**Estimated:** XS (2 tasks) | **Actual:** XS — 2 tasks, no rework, no blockers

## Summary
Delivered the persistence half that s4.3 deferred: `IScoreRepository` (`add_many`, `get_by_scenario`)
and `SQLAlchemyScoreRepository` over the pre-existing `ScoreORM` + mappers, plus a `get_score_repo`
container provider. Pure mirror of `SQLAlchemyClientRepository`. Backend 331 → 337 tests.

## What went well
- **Gemba paid off before writing scope:** confirmed `Score`, `ScoreORM`, and both mappers already
  existed (E2), so the story was correctly scoped to *only* the port + adapter + wiring — no
  speculative rebuild.
- **Followed the proven pattern exactly** (client repo), keeping the adapter trivial and review-friendly.
- **One honest design decision recorded:** scores carry their own id from the scorer (unlike clients),
  so the adapter persists `domain.id` as-is rather than reassigning — asserted by the round-trip test.

## What to improve
- **Consumer wiring is still absent:** nothing calls `add_many` yet. The epic acceptance-gate item
  "every client in an active scenario has a persisted Score" needs a scoring pipeline that persists —
  verified end-to-end at M4. This story delivered the seam; M4 must confirm it is actually used.

## Heutagogical Checkpoint
1. **What did I learn?** Scoping to the true gap (port+adapter, not the whole stack) depends entirely
   on the gemba read — the "entity/ORM/mappers already exist" claim in the epic scope was accurate and
   verifiable in one grep.
2. **What would I change about the process?** Nothing material for an XS of this shape.
3. **Framework improvements?** Same standing item: graph/PRIME returns 0 (wrong root); gemba substitutes.
4. **More capable of now?** Confident with this codebase's hexagonal persistence pattern end to end.

## Improvements applied
- None needed.

## Patterns added / reinforced
- Reinforced (implicitly): mirror the nearest proven adapter rather than inventing a new shape.

## Learning chain summary
- `.raise/rai/learnings/` absent (project-wide) — metrics N/A, unchanged by this story.
- PRIME graph queries 0 results (known wrong-root issue).

## Note for M4 / epic close
- s4.9 delivers the port+adapter; **wiring the scoring pipeline to persist** and confirming
  "every client has a persisted Score" is an M4 E2E verification item, not closed by this story alone.
