# S7.3: English translation pass scope

## In Scope
- Review and polish all English copy in `frontend/messages/en.json` across every module: sidebar, scenarios, CSV upload, operations (cases), case detail, contact result, communications, executive panel + NL query
- Fix any English value that is actually untranslated, stale, awkward, or grammatically/idiomatically off (including the ~16 key values currently identical to es.json)
- Confirm `frontend/messages/es.json` remains intact for the Spanish demo
- Verify EN/ES key parity (exact same key set, no missing/extra keys)
- Verify every user-facing component reads copy from the i18n layer (no hardcoded JSX strings)
- Verify locale switching renders the English copy at runtime
- Add/adjust i18n keys for any copy discovered during the pass that is currently hardcoded

## Out of Scope
- Translating backend messages, logs, or NL-query responses (not UI copy)
- Translating generated communication drafts (dynamically generated content)
- PWA configuration (s7.1)
- Accessibility audit (s7.2)
- Backend / DB changes; live-Postgres E2E (standing environment blocker, E7)
- Latam/native-speaker linguistic review of the Spanish (es.json) — ES is the baseline, not the target

## Done when
- `frontend/messages/en.json` renders idiomatic, polished English on every screen
- EN/ES key sets match exactly (script-verified, 0 diff)
- No user-facing string in `src/` is hardcoded outside the i18n system
- Locale switch shows English (previously verified on ES); EN shown consistently across all modules
- All gate checks pass: `vitest` green (test.sh), no TypeScript errors (typecheck.sh), lint clean (lint.sh)
- Story retrospective written