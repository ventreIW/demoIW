# S7.2 Progress — Accessibility pass (WCAG 2.1 AA)

## T1 — Design tokens + focus foundation [done]
- 63bc495 — globals.css `:root` tokens (neutral/slate), `*:focus-visible` outline; tailwind.config wired `<alpha-value>`
- fix a11y-tokens path (process.cwd) + quote-style assertion
- 3/3 a11y-tokens, 112/112, tsc clean

## T2 — Layout semantics: skip-link, landmarks, sidebar focus [done]
- 2080d50 — skip-to-content link, `<main id="main-content">` + aria-label, `<nav>` landmark, `aria-current="page"` (usePathname from @/i18n/routing), SidebarLink + LocaleSwitcher focus-visible:outline-ring
- i18n keys: app.skipToContent, app.mainContentLabel, sidebar.navLabel (ES/EN)
- 4/4 layout a11y assertions, 116/116, tsc clean

## T3 — Table semantics + contrast fixes [done]
- 1b83914 — text-slate-400 (~3.3:1 fail) -> text-slate-500 (~5.1:1) in CaseTable rank, KpiCard subLabel, QueryResultChart noData, GenerateScenarioForm help texts; CsvUpload custom buttons focus-visible:outline-ring
- Note: CaseTable already had scope="col" on all <th>; ScenarioCard badges (emerald/amber/blue/green/gray-800-on-100) all pass AA. Contrast is browser-computed -> verified by T4 axe scan, not unit tests.
- 116/116 tests, tsc clean

## T4 — Repeatable a11y audit + manual integration [done]
- 1b83914+ — axe-core ^4.13.0 as devDep; a11y test-utils helper (runAxeOn); 9-component a11y scan test asserting 0 WCAG 2.1 AA semantic violations
- Added document.lang + title setup in jsdom for accurate axe scanning (avoids document-title / html-has-lang false positives)
- 28 test files, 167 tests, tsc clean