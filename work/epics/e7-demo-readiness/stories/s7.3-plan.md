# Plan: s7.3 — English translation pass

**Story:** s7.3 English translation pass
**Epic:** E7 Demo Readiness
**Size:** S (3-5 SP)
**Complexity:** simple
**Created:** 2026-08-14

---

## Task List

### T1: Review and polish `frontend/messages/en.json`

| Field | Value |
|-------|-------|
| **Description** | Module-by-module review of all 172 keys in `frontend/messages/en.json`. Polish to idiomatic, natural English. Focus areas: (1) all 13 sections from `sidebar` through `executivePage`; (2) the ~16 keys whose values currently match es.json — confirm each is correct English (not a Spanish carryover); (3) help texts, error messages, and empty-state copy across every module. |
| **Files to modify** | `frontend/messages/en.json` |
| **AC ref** | story.md § Scenarios 1-4 (key parity, idiomatic English, module coverage) |
| **Verification** | Run key-parity script: `python3 -c "import json; en=json.load(open('frontend/messages/en.json')); es=json.load(open('frontend/messages/es.json')); print('OK' if json.dumps(en, sort_keys=True)==json.dumps(es, sort_keys=True) or set(en.keys())==set(es.keys()) else 'MISMATCH')"` — should confirm identical key sets (172/172) |
| **Size** | S |
| **Dependencies** | None |

### T2: Code cleanup + verification gates + manual switch test

| Field | Value |
|-------|-------|
| **Description** | (A) Remove the Spanish inline comment `{/* Número de clientes */}` from `frontend/src/components/scenarios/GenerateScenarioForm.tsx:108`. (B) Run full verification gate suite. (C) Manual locale-switch test: switch UI to EN, verify every module renders English copy naturally. |
| **Files to modify** | `frontend/src/components/scenarios/GenerateScenarioForm.tsx` |
| **AC ref** | Acceptence criteria AC1–AC5 (polish, comment removal, gate checks, no hardcoded strings, locale switch) |
| **Verification** | (1) `cd frontend && npx vitest run --no-coverage` — green (2) `cd frontend && node node_modules/typescript/bin/tsc --noEmit` — green (3) `cd frontend && npm run lint` — clean (4) `cd frontend && npx prettier --check .` — clean (5) Manual: Start dev server, switch locale to EN, verify all pages render English copy |
| **Size** | S |
| **Dependencies** | T1 (en.json must be polished first) |

---

## Execution Order

1. **T1** → 2. **T2**

No parallelization — T2 depends on T1. Risk-first ordering: the most impactful work (copy review) happens first. T2 is verification-only.

**Dependency graph:** `T1 -> T2` (acyclic ✓, no circular dependencies)

---

## Gate Policy

This is a **frontend-only** story. Replace standard `rai gate check` with direct commands:

| When | Command | Scope |
|------|---------|-------|
| T1 verification | `python3 -c "..."` key-parity check | en.json vs es.json |
| T2 (per task) | `cd frontend && npx vitest run --no-coverage` | Full frontend suite |
| T2 (per task) | `cd frontend && node node_modules/typescript/bin/tsc --noEmit` | Full frontend |
| T2 (per task) | `cd frontend && npm run lint` | Full frontend |
| T2 (per task) | `cd frontend && npx prettier --check .` | Full frontend |
| T2 (manual) | Start dev server, switch locale to EN | All modules |
| End of story | Same as per-task (full frontend suite) | Frontend only |

---

## Risks and Mitigations

| Risk | Likelihood | Mitigation |
|------|-----------|------------|
| WSL cross-filesystem: `tsc` or `vitest` time out | Medium | Use `--no-coverage` for vitest; fall back to `node node_modules/typescript/bin/tsc --noEmit` absolute path for tsc |
| Copy change breaks a test that asserts on specific string | Low | Vitest will catch it — fix the assertion to match the polished copy |
| Overlook a module with remaining Spanish copy | Low | Systematic 13-section pass + manual locale switch catches any misses |

---

## Duration Tracking

| Task | Estimated | Actual | Notes |
|------|-----------|--------|-------|
| T1: Polish en.json | ~15 min | | |
| T2: Cleanup + gates | ~15 min | | |
| **Total** | **~30 min** | | |