# s3.2 Scope — OpenRouter Adapter

## In scope

- `app/ports/llm_port.py` — `ILLMPort` abstract interface
- `app/adapters/llm/openrouter_adapter.py` — `OpenRouterAdapter`
- `app/domain/exceptions.py` — `ExternalServiceError` (if not already added in s2.1)
- `app/config.py` — add OpenRouter fields to `Settings`
- `.env.example` — add `OPENROUTER_API_KEY`, `OPENROUTER_BASE_URL`, `MODEL_DATA_ENRICHMENT`, `MODEL_COMMUNICATIONS`, `MODEL_NL_QUERY`
- `tests/unit/test_openrouter_adapter.py` — mocked with `respx`
- `requirements.txt` — add `respx` to dev deps

## Out of scope

- Prompt building or enrichment logic (s3.3)
- Wiring the adapter into any use case (s3.3, s3.4)
- Frontend
