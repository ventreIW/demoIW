# s3.2 Plan — OpenRouter Adapter

## Tasks

1. Test: `generate()` returns content from response JSON
   - Already implemented in `tests/unit/test_openrouter_adapter.py` as `test_generate_returns_content`

2. Test: 503 triggers retry; success on 3rd attempt
   - Create test that mocks 503, 503, 200 responses and verifies that the adapter retries twice and returns the successful response.

3. Test: 4xx raises ExternalServiceError immediately (no retry)
   - Create test that mocks a 404 response and verifies that ExternalServiceError is raised on first attempt without retry.

4. Test: structured log entry is emitted on success
   - Create test that captures log output and verifies that on successful call, a log entry with model, prompt_tokens, completion_tokens, and latency_ms is emitted.

5. Impl: `ILLMPort` + `OpenRouterAdapter` — all tests pass
   - Ensure the adapter implementation satisfies all tests (including the new ones).
   - Current implementation may need adjustments to retry/logging logic.

6. Update `Settings` with OpenRouter fields + `.env.example`
   - Add OpenRouter configuration fields to `app/config.py` (or create a new config section).
   - Update `.env.example` with the required environment variables.

## Notes
- Follow TDD: write failing tests first, then implement to make them pass.
- Use `respx` for mocking HTTP calls.
- Ensure unit tests do not make real network calls.
- Run `mypy` to ensure type checking passes.
- Commit after each task.