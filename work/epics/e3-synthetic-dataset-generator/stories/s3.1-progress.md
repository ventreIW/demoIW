# s3.1 Progress — Procedural Data Generation Layer

Branch: `story/s3.1/procedural-generator`

| Task | Status | Commit | Notes |
|------|--------|--------|-------|
| T0 — deps (numpy/pandas/faker) | ✅ done | | Installed via `uv pip install` into `backend/.venv`; import verified |
| T1 — scaffolding (port/VO/params) | ✅ done | | 4 contract tests green; mypy override for pandas added |
| T2 — ProceduralGenerator + stats | ✅ done | | 8 tests green; RNG-derived UUIDs for determinism; mypy strict clean |
| T3 — ADR-003 + smoke | ⬜ todo | | |

## Log

- **T0**: `requirements.txt` += numpy==2.2.1, pandas==2.2.3, faker==33.1.0. Venv is uv-managed (no `pip`); installed with `uv pip install --python .venv/bin/python`. Import check passed. Also synced venv to full requirements (was missing `python-multipart`).
- **T1**: created `IDatasetPort` (sync), `RawDataset` VO, `GenerationParams`, package inits. 4 contract tests pass. Added mypy override treating `pandas` as untyped (strict pandas-stubs too noisy for df-heavy layer; numpy/faker stay strict).
- **T2**: `ProceduralGenerator(IDatasetPort)`. **Design bug caught:** original pseudocode used `uuid.uuid4()` (non-deterministic) — replaced with RNG-derived UUIDs (`rng.bytes(16)`) so determinism AC holds. Days-overdue (Exp) and overdue status (Bernoulli) modelled as independent draws. 8 tests green, mypy strict clean.
- **⚠ Pre-existing gate debt (NOT s3.1):** full-project `gate-lint`/`gate-format` fail on files merged before this branch — `app/routers/scenarios.py` (W292, s2.2) and 11 frontend files (prettier, s2.3/s2.4). My s3.1 files are lint+format clean. Recommend a separate hygiene fix on `main`, not folded into this story.
