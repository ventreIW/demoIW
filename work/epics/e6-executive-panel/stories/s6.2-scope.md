## In Scope
- New `/[locale]/executive` page with KPI cards and segmentation charts
- KPI cards: total overdue, expected recoverable, cases by category, recovery rate (both definitions)
- Segmentation charts (hand-rolled SVG/CSS): days-overdue buckets, amount ranges, score categories
- API client for `GET /api/v1/scenarios/{id}/kpis` using types derived from `s6.1-payload.json`
- `scored_at` timestamp display (ADR-009) — visible staleness over silent inconsistency
- Actionable empty state for unscored scenarios (409 response) with "Score this scenario" CTA
- Sidebar executive link update: `MainLayout.tsx` `sidebar.executive` href from `#` to `/[locale]/executive`
- i18n keys for ES/EN parity in `es.json` and `en.json`
- Vitest tests with mocked API responses (no backend dependency)

## Out of Scope
- Backend KPI aggregation API (s6.1 — done by Nano)
- NL query API (s6.3) and NL query UI (s6.4)
- Sector as a segmentation dimension — structurally impossible (see E6 scope §Open questions)
- Saved dashboards, drill-through, export — beyond demo needs (parking lot)
- Real-Postgres E2E verification — standing environment blocker (E7)
- English translation pass, PWA, accessibility audit (E7)

## Done when
- Dashboard shows all four KPI cards with correct values from s6.1 aggregate
- Three segmentation charts render with data from s6.1 aggregate
- `scored_at` visible on every dashboard view
- Unscored scenario shows actionable empty state with "Score this scenario" link
- Sidebar executive link navigates to the executive page (not `#`)
- ES/EN i18n key parity holds
- `vitest` green; no TypeScript errors
- Story retrospective written