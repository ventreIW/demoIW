## In Scope
- Next.js 15 App Router init with TypeScript strict
- Tailwind CSS + shadcn/ui base setup
- Basic layout shell (sidebar navigation placeholder + main area)
- `GET /healthcheck` API route returning `{ status: "ok" }`

## Out of Scope
- Any feature UI screens
- Database tables / ORM models
- i18n (next-intl)
- PWA manifest / service worker
- Authentication / role switching
- Vercel / Railway deployment config
- Docker / docker-compose
- OpenRouter integration
- Backend-specific items: FastAPI app factory, Pydantic Settings v2, Async SQLAlchemy 2.0 engine + session factory, Alembic initialized, `GET /health` endpoint returning `{ status: "ok\", db: \"connected\" }`, Pytest config + one passing test (health endpoint)

## Done when
- `cd frontend && npm run dev` starts on port 3000 without errors
- `cd frontend && npm run typecheck` exits 0
- `cd frontend && npm run lint` exits 0
- `cd frontend && npm test -- --run` all tests pass (if any)
- The healthcheck API endpoint is accessible and returns `{ status: "ok" }`
- The basic layout shell is rendered with sidebar placeholder and main area