# s1.1 Design — Repository Scaffold & CI Pipeline

---

## CI workflow structure

```yaml
# .github/workflows/ci.yml
name: CI
on:
  pull_request:
    branches: ["main", "e*"]

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20", cache: "npm", cache-dependency-path: frontend/package-lock.json }
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test -- --run

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: pip install -r requirements-dev.txt
      - run: ruff check .
      - run: mypy app/
      - run: pytest tests/ -v --tb=short
```

Note: backend CI at this stage has no real DB — pytest uses an in-memory SQLite via a test override of the DB URL (introduced in s1.3).

---

## Key config values

**`frontend/tsconfig.json` (critical fields)**
```json
{
  "compilerOptions": {
    "strict": true,
    "paths": { "@/*": ["./src/*"] },
    "moduleResolution": "bundler"
  }
}
```

**`backend/pyproject.toml` (critical sections)**
```toml
[tool.ruff]
line-length = 100
select = ["E", "W", "F", "I", "UP"]

[tool.mypy]
strict = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```

---

## TDD tasks

1. Write: `.gitignore` — verify `git status` shows no untracked artifacts after `npm install`
2. Write: `frontend/` config files — verify `npm run typecheck` passes on empty `src/`
3. Write: `backend/pyproject.toml` + requirements — verify `ruff check .` and `mypy app/` pass on empty `app/`
4. Write: `.github/workflows/ci.yml` — verify workflow syntax via `act` or GitHub dry-run
5. Write: `README.md` setup section
6. Commit after each task (GR-GIT-004)
