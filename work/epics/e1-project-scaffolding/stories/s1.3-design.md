# s1.3 Design — Backend Skeleton

---

## App structure

```
backend/
  app/
    main.py              ← app factory
    config.py            ← Settings
    infrastructure/
      database.py        ← engine + session
      logging.py         ← structlog setup
    routers/
      health.py          ← /health
  tests/
    conftest.py          ← fixtures
    test_health.py       ← health endpoint tests
  alembic/
    env.py
    versions/
      0001_init.py       ← empty migration (no tables)
  alembic.ini
  .env.example
```

---

## Key implementation patterns

### `app/main.py`
```python
from contextlib import asynccontextmanager
from fastapi import FastAPI
from app.infrastructure.database import create_engine_and_check

@asynccontextmanager
async def lifespan(app: FastAPI):
    # startup: verify DB connectivity
    yield
    # shutdown: dispose engine

def create_app() -> FastAPI:
    app = FastAPI(title="demoIW API", lifespan=lifespan)
    app.include_router(health_router)
    return app

app = create_app()
```

### `app/infrastructure/database.py`
```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker

engine = create_async_engine(settings.database_url, echo=False)
AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)

async def get_session() -> AsyncGenerator[AsyncSession, None]:
    async with AsyncSessionLocal() as session:
        yield session
```

### `/health` endpoint
```python
@router.get("/health")
async def health(session: AsyncSession = Depends(get_session)):
    try:
        await session.execute(text("SELECT 1"))
        return {"status": "ok", "db": "connected"}
    except Exception:
        raise HTTPException(status_code=503, detail={"status": "degraded", "db": "unavailable"})
```

### Test fixture (CI-safe — no real DB)
```python
# tests/conftest.py
@pytest.fixture
async def client():
    # Override DATABASE_URL with SQLite for CI
    app.dependency_overrides[get_session] = override_get_session
    async with AsyncClient(app=app, base_url="http://test") as ac:
        yield ac
```

---

## `.env.example`

```
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/demoiw
APP_ENV=development
LOG_LEVEL=INFO
```

---

## TDD tasks

1. Test: `test_health.py` — `GET /health` returns 200 with `{"status":"ok","db":"connected"}` (fails — no app yet)
2. Impl: `config.py` + `infrastructure/database.py` — minimal Settings + engine
3. Impl: `main.py` + `routers/health.py` — verify test passes
4. Test: health DB-unavailable path — override session to raise exception, expect 503
5. Impl: exception handler in health router — verify test passes
6. Impl: `infrastructure/logging.py` — structlog JSON setup
7. Run `alembic init alembic` + configure `env.py` + create `0001_init.py`
8. Verify `mypy app/` and `ruff check .` exit 0
