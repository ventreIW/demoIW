# Parking Lot

Ideas not ready for the backlog. No commitment, no timeline. Review at each epic close.

Items that get scoped move to `governance/backlog.md`.

---

| Date added | Idea | Why parked |
|---|---|---|
| 2026-06-10 | Simulation mode — replay scenario over time (show recovery progression day by day) | High demo value, high complexity. Revisit after core modules ship. |
| 2026-06-10 | Webhook integration to external email/WhatsApp services | Out of demonstrative scope per restrictions. No real sends in demo. |
| 2026-06-10 | Multi-tenant / multi-organization support | Demonstrative scope is single-org. Future product direction only. |
| 2026-06-10 | Custom ML model training UI (upload training data, retrain in-browser) | Complex, not needed for demo conviction. |
| 2026-07-02 | RaiSE framework: `rai session start` should sync/verify backend venv against `requirements.txt` | Found in s3.1 — venv was missing `python-multipart` (committed in s2.5) and had no `pip`; only surfaced on a test import failure. |
| 2026-07-02 | RaiSE framework: fix orphaned-test check diff range in `rai-story-implement` | Uses reversed `git diff HEAD...$base`, producing false positives (flagged s3.1's own new test). Correct form: `git diff --name-only main...HEAD`. |
| 2026-07-20 | Process: make `work/backlog/` items visible to epic planning | i18n got planned three times — as E4 s4.1, as epic E9, and as backlog item `b15-i18n-setup`, which is the one that actually shipped (2026-07-08). E9 was written 5 days *later*, describing already-merged work and naming pages that do not exist. Root cause: backlog items live outside `work/epics/`, so epic planning has no view of them. Options: index delivered backlog items in the epic tracker, or require a codebase check (`rai graph query`) before writing an epic brief. |
| 2026-07-21 | RaiSE framework: design gates should ask whether the chosen approach can deliver the property it was chosen for | ADR-007 picked logistic regression *specifically* for per-feature explainability, then nothing verified the features were independent enough for attribution to be stable. They were not (0.872 correlation), and s4.3 T5 caught it three tasks later. `rai-story-design`'s lean gates cover KISS/DRY/YAGNI/MVP but not "can this approach actually do the thing it was selected for". |
| 2026-07-21 | **Nobody who does collections has read the Spanish explanations** | s4.4 ships one Spanish sentence per score (e.g. *"Buen perfil de pago: atraso contenido, de hasta 23 días, pagos parciales recibidos y 4 de 5 facturas liquidadas"*). Every automated check passes — no feature names, correct direction, deterministic, 243 distinct explanations across 271 clients. Whether a collections officer finds the register natural is a human judgement nobody has made. A five-minute review with Gustavo before the demo; if the tone is wrong, the templates are one file (`score_explainer.py`). |
| 2026-07-21 | **Policy: maximise recovery vs prevent write-offs** | The priority formula ranks by expected recoverable value, so the queue chases *collectable* money — a 30-score client needs a very large balance to outrank a 70-score one. The riskiest accounts are therefore not chased hardest, and a cheap-to-collect 83.6-score client owing $1,108 sits at rank 271 of 271. That is expected-value ranking working exactly as specified, not a bug. Deliberately left uncoded rather than hidden behind a fudge factor. Needs a decision from Gustavo; if the answer is "prevent write-offs", the formula changes and E5 workload policy inherits it. Documented in `docs/03` §8–9. |
| 2026-07-21 | RaiSE framework: designs should require evidence that a named statistical property is present in the data | s4.5 is called "Pareto filtering", which implies concentration. Measurement showed the top 20% of clients hold 40% of expected value, not 80% — the data is nearly flat. The design skill's risk table catches "is this number justified?" but not "is this phenomenon present?". For any feature named after a distribution or law (Pareto, power law, long tail, normal), the design should require evidence the data exhibits it before the feature is built around it. |
| 2026-07-21 | Generator amounts are not heavy-tailed, so the demo has no Pareto story | `ProceduralGenerator` draws invoice amounts from `Normal(amount_mean, amount_std)`. With the demo's 10,000 ± 3,000 every client holds a similar balance, so measured concentration is **top 20% of clients = 40% of expected recoverable value**, not 80% — consistent across all three sectors. Reaching 80% of value needs ~58% of accounts. Real receivables portfolios are heavy-tailed, which is what makes Pareto filtering valuable. Fix is a log-normal amount distribution in E3's generator (~1 line), but it invalidates seed-42 fixtures that s4.2/s4.3/s4.4 tests pin, so it needs its own story. s4.5 implements the filter honestly and reports the real figure. |
| 2026-07-21 | RaiSE framework: design examples should cover each branch of the output space, not just the happy path | `rai-story-design` states that examples drive code-generation accuracy more than any other section — and s4.4's examples showed only low-score explanations, so the positive-contribution case was never imagined. That omission produced the story's central defect ("Buen perfil de pago: facturas con hasta 40 días de atraso"), caught only by reading real output. The skill should prompt for one example per output branch. |
| 2026-07-21 | RaiSE framework: structural acceptance criteria should be expressed as tests, not checklist lines | s4.3's AC #15 said "no `sklearn` import outside `adapters/scoring/`". It was written down, true, and violated by the author within the same story — caught by the full suite, not by the AC. Import-boundary and layer-dependency ACs should become executable assertions at design time. **Seen twice now:** s4.4 wrote AC #12 as a test but implemented it as a substring grep, which flagged a docstring describing the rule. Two stories, two failures — this needs a mechanism, not a third note. **Fourth story now:** s4.5 made it a per-story acceptance criterion (AC #13) and it worked, catching an unused accessor before close rather than at the gate. That is a workaround for something that wants to be a repo-level check. |
| 2026-07-21 | Pattern store is write-only in practice | Four patterns were added in s4.2 and none could be surfaced by PRIME during s4.3, because graph queries return another project's files. Until the graph is rebuilt against the correct root, `rai pattern add` accumulates knowledge nothing reads back. |
| 2026-07-21 | RaiSE framework: gemba step should *run* data-processing code, not only read it | s4.2's two late findings — `sector` is constant within a scenario, and 32–43% of clients are excluded for having nothing outstanding — are properties of the output *distribution*, invisible to code reading but obvious in one execution. `rai-story-design`'s gemba step says "read the modules"; for data stories it should add "generate a sample and describe the frame". Both findings would have moved from T5 to design. |
| 2026-07-21 | RaiSE framework: `.raise/rai/learnings/` never created, so review's chain-read has nothing to aggregate | `rai-story-review` Step 3 reads design/plan/implement learning records; the directory does not exist in this project, so acceptance rate, gap rate and pattern utility are all N/A. Either the CLI should create it or the skills should stop assuming it. |
| 2026-07-21 | Knowledge graph indexes content outside this repository | `rai graph query` during E4 PRIME returned a hit from `/mnt/c/Users/.../Auto_Reporte/.claude/skills/` — a different project. Orientation queries are unreliable until the graph is rebuilt with the correct root. |
| 2026-07-21 | `GenerateDataset` bypasses `IDatasetPort` | The use case imports and instantiates `ProceduralGenerator` directly (`app/application/use_cases/generate_dataset.py`) instead of receiving `IDatasetPort` by injection. The port exists but is unused for the generator, so the application layer depends on a concrete adapter — the coupling the hexagonal structure is meant to prevent. Found during s4.2 gemba; out of scope there. Small fix: inject via `container.py` like the repositories. |
| 2026-07-20 | Intermittent frontend test failure — 1 failure in ~30 runs, not reproduced | Observed once during E3 close: `Test Files 1 failed \| 9 passed`, `Tests 1 failed \| 44 passed`. Then 27 consecutive clean runs; the failing test name was not captured. Same suite already produced one confirmed async leak (`ScenarioGrid` fetch past teardown, fixed in PR #1), so a second unfixed race is plausible — `GenerateScenarioForm`'s async tests are the likeliest candidate. Watch CI; if it recurs, capture with `vitest --run --reporter=verbose` and check for un-awaited promises / missing `await waitFor`. |
| 2026-07-20 | Dead `/api/v1/scenarios/{id}/download` endpoint behind a live UI button | `GenerateScenarioForm` renders a "Descargar JSON" link to an endpoint that does not exist — clicking it 404s. Its test passes because it only asserts the `href` string, never that the route resolves. Found at E3 close; not in E3 scope. Either implement the export or remove the button. |
| 2026-07-20 | Guard against untranslated UI shipping after i18n landed | `GenerateScenarioForm.tsx` (s3.4) shipped 27 hardcoded Spanish strings and a fixed `toLocaleDateString('es-MX')` after `b15` established i18n. Nothing flagged it. Consider an ESLint rule for literal user-facing strings in JSX, or a review-checklist item. Retrofit tracked as E4 s4.7. |
| 2026-07-27 | `OpenRouterAdapter` raises `KeyError` on a well-formed-looking error body | `_call` does `data["choices"][0]...` unconditionally. Free-tier models occasionally return HTTP 200 with a body lacking `choices` (or an `{"error": …}` payload); observed once during s4.8 verification (first attempt `KeyError: 'choices'`, spaced retry succeeded). Should treat a missing-`choices` body as a failure → `ExternalServiceError`, so enrichment degrades gracefully instead of crashing. Out of s4.8 scope (adapter hardening excluded). Own story; also relevant to E5 comms / E6 NL query which share the adapter. |
| 2026-07-27 | Generating a scenario with a pinned `reference_date` crashes on persist | `GenerateDataset` stores `parameters=params.model_dump()` in the `scenarios.parameters` **JSON** column; when `reference_date` is set, `model_dump()` yields a `datetime.date`, which the JSON serializer rejects: *"Object of type date is not JSON serializable"*. All existing tests pass `reference_date=None`, so it was never hit — the **M4 in-process E2E caught it** on the first run with a pinned date. Impact: cross-day reproducibility (the whole point of pinning `reference_date`, per its own docstring) is currently broken. Fix: serialize dates in `parameters` (e.g. `model_dump(mode="json")`) before persisting. Small, but a real generation-layer bug — own bugfix story. |
| 2026-07-27 | Enrichment descriptions come back in English for a Spanish-UI demo | The prompt `prompts/data_enrichment/v1_company_description.txt` doesn't pin a language; Nemotron returned English `sector_description`s during s4.8 verification. Prompt-tuning was explicitly out of s4.8 scope (verification first). One-line prompt change to require Spanish; verify output reads naturally before the demo. |
| 2026-07-28 | `?category=High` on `/prioritized` silently returns zero cases | The router validates `category` against `{"High","Medium","Low"}` but `ScoreCategory` is a `StrEnum` whose values are lowercase (`"high"`), and the response serializes `c.category.value`. So the comparison never matches and the filter returns an empty portfolio for every valid input. **Exactly the same bug species as the `days_overdue` filter fixed in s5.1**, and hidden the same way: `test_category_filter` asserts `case["category"] == "High"` inside a `for` loop over an empty list (vacuous), then asserts `value_share >= threshold` — which passes because `value_share` is computed from the *unfiltered* portfolio. Confirmed against the running app. Fix is one line (compare against the enum value, or normalise case). Not fixed in s5.1: no s5.1 AC needs the filter and sort/filter UI is explicitly out of scope. **Recommended next bugfix story** — and worth grepping for other filters asserted only by vacuous loops. |
| 2026-07-28 | CSV-uploaded scenarios can never be scored or prioritized | `sqlalchemy_scenario_repo.create_from_csv` writes every invoice with `status="pending"`, but `feature_extractor` treats only `"overdue"` as open and `"paid"` as settled. Every CSV invoice is therefore invisible to the extractor, so `/prioritized` fails with `InsufficientTrainingDataError: "no clients with an outstanding balance — nothing to label. Every invoice in this scenario is settled."` Found while trying to use the CSV path for s5.1's manual integration test (the generate path needs OpenRouter). Impact: the CSV upload feature produces scenarios that no downstream module can use. Fix: write a status the extractor recognises, derived from due date. Own bugfix story. |
| 2026-07-28 | `POST /scenarios/generate` returns 500 on an OpenRouter `ReadTimeout` | Observed once during s5.1 manual verification: `httpx.ReadTimeout` propagated to a 500. A retry succeeded and degraded honestly (`enriched: false`), so s4.8's degradation works for *failed* responses but not for *timeouts*. Same adapter-hardening area as the `KeyError` entry above — likely one story covering both. |
| 2026-07-28 | Scenarios page's active-card highlight never lights up | `scenarios/page.tsx` reads an `active_scenario_id` cookie that nothing in the app ever sets (s5.1 removed the middleware guard that read the same phantom cookie). The backend persists `status` via `PATCH /activate`, so the page should resolve the active card from `status === 'active'` as `lib/api/prioritized.getActiveScenario` now does. Cosmetic, out of s5.1 scope (scenarios page is not this story's surface). |
| 2026-07-28 | RaiSE tooling friction observed during s5.1 (framework, not product) | Four items, each costing a round-trip: (a) `rai gate check --all` does not complete within 120s in this project — use scoped `gate-tests` instead; (b) `rai gate check gate-tests --scope <path>` resolves paths **relative to `backend/`**, not the repo root, and accepts only one path, so `--scope backend/tests/` fails with "file or directory not found"; (c) `rai artifact emit` / `rai docs write` do not exist in CLI v3.0.0 although `/rai-story-design` and `/rai-story-plan` specify them as the primary output — every story so far has used the Markdown fallback; (d) the `/rai-story-implement` orphaned-test check interpolates paths into a `grep` pattern, so Next.js `[locale]` directories become a character class and produce false positives — needs `grep -F`. Also standing from E4: `.raise/rai/learnings/` is absent project-wide (learning-chain metrics N/A) and `rai session context -s code_context` returns 0 symbols for `story/...` branch names. |
| 2026-08-04 | **Per-client sector, so segmentation has a business dimension** | Found in E6 design gemba (G2). `Client` carries only free-text `sector_description`; `Sector` is an attribute of `Scenario`, so every client in a scenario shares one sector. RF-06.2 names sector as an example segmentation dimension and the E6 brief's headline NL question is "¿Cuánto está vencido en retail?" — both assume it varies. E6 ships days-overdue bucket, amount range and score category instead (RF-06.2 says "e.g.", so it is satisfied), but the *demo story* is weaker: a director asking about a sector is the natural question. Fix is a per-client sector in E3's generator, which invalidates the seed-42 fixtures s4.2/s4.3/s4.4 pin — same blast radius as the log-normal amounts item above, and probably the same story. |
| 2026-08-04 | Converge `/prioritized` onto persisted scores | ADR-009 decided E6's KPIs read persisted `Score` rows while `/prioritized` keeps re-fitting a model per request, so the dashboard and the operator queue can drift (E6 surfaces `scored_at` to make the drift visible rather than silent). Closing it properly means changing E5's delivered spine 10 days before the demo — declined on timing, not merit. Promoted from the E4-close parking-lot note to a scoped E7/E8 item. |
| 2026-08-04 | RaiSE framework: `rai graph query` fails with `OperationalError: no such column: data_json` | E6 design PRIME could retrieve nothing — the graph DB's `graph_nodes` table predates the schema the installed CLI (3.0.0) reads. Third graph-related entry: the store also indexes another project's files, and the pattern store is write-only in practice (both above). PRIME's tier-1 queries have now degraded to no-ops for two consecutive epics. Either the DB needs a rebuild/migration or the skills should stop treating graph retrieval as a routine step. |
| 2026-08-04 | RaiSE framework: `rai-story-implement` replays one developer's git identity as a universal rule | Its embedded pitfalls include "Before ANY commit in this project, confirm git identity matches" `bernardo-525` / `bernardojgm05@gmail.com`, and "If it shows any other identity (especially `ventreIW`), **STOP** — do not commit." `ventreIW` is this repository's owner. Followed literally, the skill blocks the legitimate author from committing to his own repo. Found during s6.1. The general problem is bigger than this line: skill pitfalls captured from a single session are being written back as project-wide imperatives with no marker of whose session they came from or when they applied. Either scope captured pitfalls to their origin, or review them before they enter a skill. |
| 2026-08-04 | `ruff format --check` is red project-wide on two s5.4 test files | `backend/tests/unit/test_communication_draft_service.py` and `backend/tests/integration/test_communications_integration.py` fail the format gate. Both predate s6.1 and were left untouched as out of scope, but the consequence is that **anyone running `rai gate check gate-format` unscoped gets a failure that has nothing to do with their story** — and the natural response to a red gate you did not cause is to stop trusting it. Two-second fix (`ruff format` on both), deliberately not taken during a story close, which is merge-only. Should be swept before the next story starts. |
